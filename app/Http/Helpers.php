<?php

if (! function_exists('structure_convert')) {

    /**
     * Convert format structure into correct format
     *
     * @param $structure
     * @return mixed
     */
    function structure_convert($structure) {

        return $structure;
    }
}